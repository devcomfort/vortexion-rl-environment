# @package _global_
# Main configuration file
# This file imports all configuration groups

defaults:
  - app
  - stage
  - game
  - graphics

# Configuration can be overridden here or via command line
# Example: python main.py app.width=512 app.height=384

